import React from "react";
import Hero from "../components/Hero";

const Home: React.FC = () => {
  return (
    <div style={{ backgroundColor: "#f0f0f0", width: "100%" }}>
      <Hero />
    </div>
  );
};

export default Home;
